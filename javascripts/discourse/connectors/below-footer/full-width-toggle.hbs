{{#if this.shouldRender}}
  <DButton
    {{did-insert this.toggleClasses}}
    {{will-destroy this.removeClasses}}
    @action={{action "toggleFullWidth"}}
    class="btn-flat btn full-width-toggle"
    @icon={{if this.fullWidthEnabled "compress" "expand"}}
  />
{{/if}}